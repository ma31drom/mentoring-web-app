jar {
    baseName = 'service'
    version =  '1.0'
}

dependencies {
	compile project(':dao')
	compile group: 'com.sun.mail', name: 'javax.mail', version: '1.5.5'
	testCompile 'junit:junit:4.12'
}

test.useJUnit()